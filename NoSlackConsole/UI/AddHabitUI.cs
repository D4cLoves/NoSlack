using NoSlackConsole.Services;

namespace NoSlackConsole.UI;

public class AddHabitUI
{
    // ApiClient client = new ApiClient();
    // var created = await client.Create('читать');
    // Console.WriteLine($"Добавлено: {created.Name} (ID: {created.Id})");
}   